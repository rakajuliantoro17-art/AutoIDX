/**
==========================================================
AURA Trade OS
Activity View (sub-component)
Version : 0.0.1 Alpha
==========================================================
*/
import ActivityLogs from "@/components/ActivityLogs";

export default function ActivityView() {
  return <ActivityLogs logs={[]} />;
}
